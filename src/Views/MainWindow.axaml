<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:FH6RB.ViewModels"
        xmlns:svc="using:FH6RB.Services"
        xmlns:cv="using:FH6RB.Converters"
        xmlns:controls="using:FH6RB.Controls"
        xmlns:assets="clr-namespace:FH6RB.Assets"
        x:Class="FH6RB.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Width="1000" MinWidth="1000" MaxWidth="1000" Height="800" MinHeight="650"
        Background="{DynamicResource Bg1}"
        Icon="avares://FH6RB/Assets/app.ico"
        Title="{x:Static assets:Str.TitleMain}">
    
    <Window.Styles>
        <Style Selector="StackPanel.statbar > TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TxtFaint}"/>
        </Style>
        <Style Selector="StackPanel.statbar > TextBlock.low">
            <Setter Property="Foreground" Value="#E0654F"/>
        </Style>
        <Style Selector="StackPanel.statbar > TextBlock.warn">
            <Setter Property="Foreground" Value="#D9A441"/>
        </Style>
    </Window.Styles>

    <Grid RowDefinitions="Auto,*,Auto,Auto">
        <Border Grid.Row="0" Background="{DynamicResource Bg2}"
                BorderBrush="{DynamicResource LineSoft}" BorderThickness="0,0,0,1" Padding="10, 12, 14, 12">
            <Grid ColumnDefinitions="Auto,Auto,Auto,*">
                <StackPanel Grid.Column="0" Margin="0,0,6,0">
                    <TextBlock Classes="lbl" Text="{x:Static assets:Str.SecLocalization}"/>
                    <ComboBox Width="165" ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate x:DataType="vm:LangOption">
                                <TextBlock Text="{Binding Display}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,6,0">
                    <TextBlock Classes="lbl" Text="{x:Static assets:Str.SecStation}"/>
                    <ComboBox Width="220" ItemsSource="{Binding Stations}" SelectedItem="{Binding SelectedStation}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate x:DataType="svc:StationInfo">
                                <TextBlock Text="{Binding Display}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,6,0">
                    <TextBlock Classes="lbl" Text="{x:Static assets:Str.SecBank}"/>
                    <ComboBox Width="100" ItemsSource="{Binding Variants}" SelectedItem="{Binding SelectedVariant}"/>
                </StackPanel>
                
                <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right"
                            VerticalAlignment="Bottom" Spacing="6">
                    <controls:SearchBox VerticalAlignment="Center"
                                        SearchText="{Binding SearchText}"/>

                    <Button Classes="act" Width="32" Height="32" VerticalAlignment="Center"
                            Background="{DynamicResource BgInput}"
                            ToolTip.Tip="{x:Static assets:Str.TipMenu}">
                        <Image Source="avares://FH6RB/Assets/icons/icon_more.png" Width="16" Height="16"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Button.Flyout>
                            <MenuFlyout VerticalOffset="4" Placement="BottomEdgeAlignedRight">
                                <MenuItem Header="{x:Static assets:Str.MenuRestoreStation}" Click="OnRestoreStation"/>
                                <MenuItem Header="{x:Static assets:Str.MenuRestoreAll}" Click="OnRestoreAllStations"
                                          IsEnabled="{Binding CanRestoreAll}"/>
                                <Separator/>
                                <MenuItem Header="{x:Static assets:Str.MenuSaveBackup}" Click="OnSaveBackup"/>
                                <MenuItem Header="{x:Static assets:Str.MenuLoadBackup}" Click="OnBackups"/>
                                <Separator/>
                                <MenuItem Header="{x:Static assets:Str.MenuExport}" Click="OnExport"/>
                                <MenuItem Header="{x:Static assets:Str.MenuImport}" Click="OnImport"/>
                                <Separator/>
                                <MenuItem Header="{x:Static assets:Str.MenuSaveAllMarkers}" Click="OnSaveAllMarkers"/>
                                <MenuItem Header="{x:Static assets:Str.MenuResetMarkers}" Click="OnResetAllMarkers"/>
                                <MenuItem Header="{x:Static assets:Str.MenuMarkerSettings}" Click="OnMarkerDefaults"/>
                                <Separator/>
                                <MenuItem Header="{x:Static assets:Str.MenuSettings}" Click="OnSettings"/>
                            </MenuFlyout>
                        </Button.Flyout>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <Grid Grid.Row="1" ClipToBounds="True">
            <Border Padding="10,10,0,6">
                <ListBox ItemsSource="{Binding FilteredTracks}" Background="Transparent"
                         SelectionMode="Single" SelectionChanged="OnTracksSelectionChanged"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                            <DataTemplate x:DataType="vm:TrackItemViewModel">
                                <Border Classes="row">
                                    <Grid ColumnDefinitions="Auto,*,Auto">
                                        <Button Grid.Column="0" Classes="act" IsVisible="{Binding CanPlay}"
                                                Margin="0,0,12,0" VerticalAlignment="Center"
                                                Command="{Binding $parent[ItemsControl].((vm:MainWindowViewModel)DataContext).PlayTrackCommand}"
                                                CommandParameter="{Binding}">
                                            <Panel>
                                                <Image Source="avares://FH6RB/Assets/icons/icon_play.png" Width="24" Height="24"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       IsVisible="{Binding ShowPlayIcon}"
                                                       ToolTip.Tip="{x:Static assets:Str.TipPlay}"/>
                                                <Image Source="avares://FH6RB/Assets/icons/icon_pause.png" Width="24" Height="24"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       IsVisible="{Binding IsPlayingNow}"
                                                       ToolTip.Tip="{x:Static assets:Str.TipPause}"/>
                                                <Image Source="avares://FH6RB/Assets/icons/icon_decoding.png" Width="24" Height="24"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="50%,50%"
                                                       Classes.spin="{Binding IsLoadingPlay}"
                                                       IsVisible="{Binding IsLoadingPlay}"/>
                                            </Panel>
                                        </Button>
                                        <Grid Grid.Column="1" RowDefinitions="16,4,12" VerticalAlignment="Center" HorizontalAlignment="Left" Width="710" MaxWidth="710" Margin="0 0 12 0"
                                               MinHeight="32" Opacity="{Binding Enabled, Converter={x:Static cv:Conv.EnabledOpacity}}">
                                            
                                            <Grid Grid.Row="0" Height="16" ClipToBounds="True">
                                                <Grid ColumnDefinitions="Auto,*" VerticalAlignment="Bottom">
                                                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Border IsVisible="{Binding IsUnbuilt}"
                                                                Background="{DynamicResource AccentHi}" CornerRadius="3"
                                                                Height="14" Margin="0,0,6,0" VerticalAlignment="Center">
                                                            <TextBlock Text="{x:Static assets:Str.BadgeNew}" FontSize="8" FontWeight="Bold" Foreground="White"
                                                                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="3,0,3,0"/>
                                                        </Border>
                                                        <Border IsVisible="{Binding ShowRplBadge}"
                                                                Background="{DynamicResource AccentHi}" CornerRadius="3"
                                                                Height="14" Margin="0,0,6,0" VerticalAlignment="Center">
                                                            <TextBlock Text="{x:Static assets:Str.BadgeReplaced}" FontSize="8" FontWeight="Bold" Foreground="White"
                                                                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="3,0,3,0"/>
                                                        </Border>
                                                        <Border IsVisible="{Binding ShowCstBadge}"
                                                                Background="{DynamicResource BgModifier}" CornerRadius="3"
                                                                Height="14" Margin="0,0,6,0" VerticalAlignment="Center">
                                                            <TextBlock Text="{x:Static assets:Str.BadgeCustom}" FontSize="8" FontWeight="Bold" Foreground="White"
                                                                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="3,0,3,0"/>
                                                        </Border>
                                                    </StackPanel>
                                                    
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" Spacing="10" ClipToBounds="True" Margin="0 0 12 0">
                                                        <TextBlock Text="{Binding Title}" MaxWidth="480" FontSize="14" VerticalAlignment="Bottom"
                                                                   FontWeight="SemiBold" Foreground="{DynamicResource Header}" TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding Artist}" FontSize="12" MaxWidth="220" VerticalAlignment="Bottom"
                                                                   Foreground="{DynamicResource TxtMute}" TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Grid>

                                            <Grid Grid.Row="2" Height="12" ClipToBounds="True">
                                                <TextBlock Text="{Binding InfoLine}" VerticalAlignment="Top"
                                                           FontFamily="{DynamicResource MonoFont}" FontSize="11"
                                                           Foreground="{DynamicResource TxtFaint}" TextTrimming="CharacterEllipsis"/>
                                            </Grid>
                                        </Grid>

                                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6">
                                            <Button Classes="act" IsVisible="{Binding CanDelete}" ToolTip.Tip="{x:Static assets:Str.TipDelete}"
                                                    Command="{Binding $parent[ItemsControl].((vm:MainWindowViewModel)DataContext).DeleteCommand}"
                                                    CommandParameter="{Binding}">
                                                <Image Source="avares://FH6RB/Assets/icons/icon_delete.png" Width="24" Height="24"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                            </Button>
                                            <Button Classes="act" IsVisible="{Binding CanRestoreDefault}"
                                                    ToolTip.Tip="{x:Static assets:Str.TipRestoreTrack}"
                                                    Click="OnRestoreDefault" Tag="{Binding}">
                                                <Image Source="avares://FH6RB/Assets/icons/icon_revert.png" Width="26" Height="26"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Button>
                                            <Button Classes="act" ToolTip.Tip="{x:Static assets:Str.TipReplaceAudio}" Click="OnReplace" Tag="{Binding}"
                                                    IsVisible="{Binding CanReplace}">
                                                <Image Source="avares://FH6RB/Assets/icons/icon_replace.png" Width="24" Height="24"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       RenderTransformOrigin="50%,50%"/>
                                            </Button>
                                            <Button Classes="act" ToolTip.Tip="{x:Static assets:Str.TipMarkers}" Click="OnMarkers" Tag="{Binding}">
                                                <Panel>
                                                    <Image Source="avares://FH6RB/Assets/icons/icon_pin.png" Width="18" Height="18"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="50%,50%"
                                                           IsVisible="{Binding !MarkersLoading}"/>
                                                    <Image Source="avares://FH6RB/Assets/icons/icon_decoding.png" Width="24" Height="24"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="50%,50%"
                                                           Classes.spin="{Binding MarkersLoading}"
                                                           IsVisible="{Binding MarkersLoading}"/>
                                                </Panel>
                                            </Button>
                                            <Button Classes="act" ToolTip.Tip="{x:Static assets:Str.TipEdit}" Click="OnEdit" Tag="{Binding}">
                                                <Image Source="avares://FH6RB/Assets/icons/icon_edit.png" Width="26" Height="26"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Button>
                                            <Button Classes="act" Click="OnToggle" Tag="{Binding}"
                                                    ToolTip.Tip="{Binding Enabled, Converter={x:Static cv:Conv.ToggleToolTip}}">
                                                <Image Source="avares://FH6RB/Assets/icons/icon_toggle.png" Width="24" Height="24"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       Opacity="{Binding Enabled, Converter={x:Static cv:Conv.EnabledOpacity}}"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

            <Border IsVisible="{Binding IsLoading}" Background="{DynamicResource Bg1}">
                <TextBlock Text="{x:Static assets:Str.Loading}" Foreground="{DynamicResource TxtMute}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>
        
        <Border Grid.Row="2" Background="{DynamicResource Bg2}"
                BorderBrush="{DynamicResource LineSoft}" BorderThickness="0,1,0,0" Padding="16,16">
            <Grid ColumnDefinitions="Auto,*,Auto" VerticalAlignment="Center">
                <Button Grid.Column="0" Classes="big" Width="100" MaxWidth="100" Click="OnAddTrack" IsEnabled="{Binding !IsBuilding}" HorizontalContentAlignment="Center"
                        Padding="0" Height="48" MaxHeight="48" ToolTip.Tip="{x:Static assets:Str.TipAddTrack}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Image Source="avares://FH6RB/Assets/icons/icon_add.png" Width="20" Height="20"
                               HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="50%,50%"/>
                        <TextBlock Text="{x:Static assets:Str.BtnAddLabel}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <Grid Grid.Column="1" ColumnDefinitions="Auto,*,Auto"
                      VerticalAlignment="Center" IsVisible="{Binding TransportVisible}" 
                      MaxWidth="550" Width="550" Height="48" MaxHeight="48">

                    <Button Grid.Column="0" Classes="big" Width="48" Height="48" MinWidth="48" VerticalAlignment="Center"
                            Command="{Binding TogglePlayCommand}" ToolTip.Tip="{x:Static assets:Str.TipPlayPause}"
                            Margin="0 0 16 0">
                        <Panel>
                            <Image Source="avares://FH6RB/Assets/icons/icon_play.png" Width="24" Height="24"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   IsVisible="{Binding !TransportPlaying}"/>
                            <Image Source="avares://FH6RB/Assets/icons/icon_pause.png" Width="24" Height="24"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   IsVisible="{Binding TransportPlaying}"/>
                        </Panel>
                    </Button>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2"  Width="420" MaxWidth="420" Height="48" MaxHeight="48">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="12" ClipToBounds="True">
                            <TextBlock Text="{Binding NowTitle}" MaxWidth="300" FontSize="14" VerticalAlignment="Bottom"
                                       FontWeight="SemiBold" Foreground="{DynamicResource Header}" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding NowArtist}" FontSize="12" MaxWidth="108" VerticalAlignment="Bottom"
                                       Foreground="{DynamicResource TxtMute}" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <Slider Classes="progress" Minimum="0" Maximum="{Binding DurationSeconds}" Value="{Binding PositionSeconds}"
                                VerticalAlignment="Center" Margin="0,-12,0,0"/>

                        <Grid ColumnDefinitions="Auto,*,Auto">
                            <TextBlock Grid.Column="0" Text="{Binding NowPositionText}" FontFamily="{DynamicResource MonoFont}"
                                       FontSize="11" Foreground="{DynamicResource TxtFaint}"/>
                            <TextBlock Grid.Column="2" Text="{Binding NowDurationText}" FontFamily="{DynamicResource MonoFont}"
                                       FontSize="11" Foreground="{DynamicResource TxtFaint}"/>
                        </Grid>
                    </StackPanel>

                    <Button Grid.Column="2" Classes="big" Width="48" Height="48" MinWidth="48" VerticalAlignment="Center"
                            Click="OnTransportEdit" ToolTip.Tip="{x:Static assets:Str.TipEdit}" Margin="16 0 0 0">
                        <Image Source="avares://FH6RB/Assets/icons/icon_edit.png" Width="26" Height="26"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </Grid>

                <Button Grid.Column="2" Classes="big primary" Width="100" MaxWidth="100" Click="OnBuild" IsEnabled="{Binding CanBuild}" HorizontalContentAlignment="Center"
                        Height="48" MaxHeight="48" Padding="0" ToolTip.Tip="{x:Static assets:Str.TipBuildBank}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Image Source="avares://FH6RB/Assets/icons/icon_build.png" Width="16" Height="16"
                               HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="50%,50%"/>
                        <TextBlock Text="{x:Static assets:Str.BtnBuildLabel}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
        
        <Border Grid.Row="3" Background="{DynamicResource BgDarkest}"
                BorderBrush="{DynamicResource LineSoft}" BorderThickness="0,1,0,0" Height="30" Padding="14,0">
            <Grid ColumnDefinitions="*,Auto" VerticalAlignment="Center">
                <TextBlock Grid.Column="0" Text="{Binding Status}" FontFamily="{DynamicResource MonoFont}"
                           FontSize="11.5" Foreground="{DynamicResource TxtMute}" VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Classes="statbar"
                            Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding StatusHeadText}" FontFamily="{DynamicResource MonoFont}" FontSize="11.5"/>
                    <TextBlock Text="{Binding StatusCustomText}" FontFamily="{DynamicResource MonoFont}" FontSize="11.5"/>
                    <TextBlock Text=" · " FontFamily="{DynamicResource MonoFont}" FontSize="11.5"/>
                    <TextBlock Text="{Binding StatusPlaylistText}" FontFamily="{DynamicResource MonoFont}" FontSize="11.5"
                               Classes.low="{Binding PlaylistLow}" ToolTip.Tip="{Binding PlaylistTip}"/>
                    <TextBlock Text=" · " FontFamily="{DynamicResource MonoFont}" FontSize="11.5"/>
                    <TextBlock Text="{Binding StatusSizeText}" FontFamily="{DynamicResource MonoFont}" FontSize="11.5"
                               Classes.warn="{Binding BankSizeHigh}" ToolTip.Tip="{x:Static assets:Str.TipStatusSize}"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <Border Grid.Row="0" Grid.RowSpan="4" IsVisible="{Binding IsBusy}"
                Background="#D8161719">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="14">
                <Image Source="avares://FH6RB/Assets/icons/icon_processing.png" Width="64" Height="64"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       RenderTransformOrigin="50%,50%" Classes.spin="{Binding IsBusy}"/>
                <StackPanel Spacing="4">
                    <TextBlock Text="{Binding BusyText}" Foreground="{DynamicResource Header}" FontSize="14"
                               HorizontalAlignment="Center" TextAlignment="Center"/>
                    <TextBlock Text="{Binding StatusDetail}" Foreground="{DynamicResource TxtMute}" FontSize="13"
                               FontFamily="{DynamicResource MonoFont}" HorizontalAlignment="Center" TextAlignment="Center"
                               IsVisible="{Binding HasStatusDetail}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
